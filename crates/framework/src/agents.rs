//! Agent 类型注册模块（占位符——待框架完善后实现）。

// Placeholder: the agent registry / type system will be implemented in a future update.
pub struct AgentTypeRegistry;

impl AgentTypeRegistry {
    pub fn new() -> Self {
        Self
    }
}
